---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Container from '../Container.astro';
import Button from '../Button.astro';
import Card from '../Card.astro';
import TrackCard from '../TrackCard.astro';
import { getLangFromUrl, useTranslations, localizePath } from '../../i18n/utils';
import { tracks } from '../../data/tracks';
import { site } from '../../data/site';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Placeholder featured work — replaced by the projects collection in M3.
// TODO: verify details when the real case studies are written.
const featured = [
  {
    tag: { en: 'Hardware capstone', uk: 'Hardware capstone' },
    title: { en: 'Connected-device capstone', uk: 'Connected-device capstone' },
    blurb: {
      en: 'A device → cloud → app build that ties the whole hardware course together.',
      uk: 'Збірка device → cloud → app, що поєднує весь курс із hardware.',
    },
    span: 'lg:col-span-3 lg:row-span-2',
  },
  {
    tag: { en: 'PM case study', uk: 'PM case study' },
    title: { en: 'Clinical decision support', uk: 'Clinical decision support' },
    blurb: {
      en: 'Earlier cancer detection for 20k+ clinicians in a regulated setting.',
      uk: 'Раннє виявлення раку для 20k+ клініцистів у regulated середовищі.',
    },
    span: 'lg:col-span-3',
  },
  {
    tag: { en: 'Experiment', uk: 'Experiment' },
    title: { en: 'Live-coded experiments', uk: 'Live-coded experiments' },
    blurb: {
      en: 'A home for small, embeddable demos as I learn to build.',
      uk: 'Місце для невеликих embeddable демо, поки я вчуся будувати.',
    },
    span: 'lg:col-span-3',
  },
];
---
<BaseLayout>
  <!-- Hero -->
  <Container class="pt-20 pb-16 sm:pt-28">
    <p class="text-sm font-medium uppercase tracking-widest text-accent">{t('site.role')}</p>
    <h1 class="mt-4 max-w-4xl text-display">{t('site.title')}</h1>
    <p class="mt-6 max-w-2xl text-lead text-muted">{t('site.tagline')}</p>
    <div class="mt-9 flex flex-wrap gap-3">
      <Button href={localizePath('/projects', lang)} variant="primary">{t('cta.viewWork')}</Button>
      <Button href={localizePath('/learn', lang)} variant="secondary">{t('cta.readHub')}</Button>
      <Button href={site.linkedin} variant="ghost">{t('cta.connect')}</Button>
    </div>
  </Container>

  <!-- Featured work (bento — used once, here) -->
  <Container class="py-12">
    <div class="mb-6 flex items-end justify-between gap-4">
      <h2 class="text-h2">{t('home.featured.title')}</h2>
      <a href={localizePath('/projects', lang)} class="link-underline shrink-0 text-sm text-fg/70">{t('home.featured.viewAll')}</a>
    </div>
    <div class="grid auto-rows-[minmax(11rem,auto)] grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-6">
      {featured.map((p) => (
        <Card href={localizePath('/projects', lang)} class={`flex flex-col justify-between ${p.span}`}>
          <p class="text-xs font-medium uppercase tracking-wider text-accent">{p.tag[lang]}</p>
          <div class="mt-6">
            <h3 class="text-h3">{p.title[lang]}</h3>
            <p class="mt-2 text-sm leading-relaxed text-muted">{p.blurb[lang]}</p>
          </div>
        </Card>
      ))}
    </div>
  </Container>

  <!-- Learning Hub teaser -->
  <Container class="py-12">
    <div class="mb-2 flex items-end justify-between gap-4">
      <h2 class="text-h2">{t('home.exploring.title')}</h2>
      <a href={localizePath('/learn', lang)} class="link-underline shrink-0 text-sm text-fg/70">{t('home.exploring.cta')}</a>
    </div>
    <p class="mb-6 max-w-2xl text-muted">{t('home.exploring.body')}</p>
    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {tracks.map((track) => <TrackCard track={track} />)}
    </div>
  </Container>

  <!-- Latest writing -->
  <Container class="py-12">
    <div class="mb-6 flex items-end justify-between gap-4">
      <h2 class="text-h2">{t('home.writing.title')}</h2>
      <a href={localizePath('/writing', lang)} class="link-underline shrink-0 text-sm text-fg/70">{t('home.writing.viewAll')}</a>
    </div>
    <Card class="text-muted">{t('home.writing.empty')}</Card>
  </Container>

  <!-- Closing CTA -->
  <Container class="py-16">
    <div class="rounded-[var(--radius-card)] border border-line bg-surface px-8 py-12 text-center">
      <h2 class="text-h2">{t('home.closing.title')}</h2>
      <p class="mx-auto mt-3 max-w-md text-muted">{t('home.closing.body')}</p>
      <div class="mt-7 flex flex-wrap justify-center gap-3">
        <Button href={site.linkedin} variant="primary">{t('cta.connect')}</Button>
        <Button href={`mailto:${site.email}`} variant="secondary">{t('footer.email')}</Button>
      </div>
    </div>
  </Container>
</BaseLayout>
